package com.atiq.mp.core

import androidx.compose.runtime.Composable

@Composable
actual fun StatusBarAppearance(isBackgroundLight: Boolean) {
    // Ios system automatically handles status bar appearance according to the background.
}